import React, {Dispatch, SetStateAction} from "react";

/*import {FilterType} from "../App";

type MoneyComponentType = {
    currentMoney: Array<MoneyType>
    setMoney: Dispatch<SetStateAction<{ banknote: string; nominal: number; number: string; }[]>>
    onClickFilterHandler: (name: FilterType) => void
}

type MoneyType = {
    banknote: string,
    nominal: number,
    number: string,
}

type OnClickFilterHandlerType = {
    name: string,
    callBack: () => void
}


export const MoneyComponent = (props: MoneyComponentType) => {
    return (
        <>
            <ul>
                {props.currentMoney.map((objFromMoneyArr, index) => {
                    return (
                        <li key={index}>
                            <span>{objFromMoneyArr.banknote} </span>
                            <span>{objFromMoneyArr.nominal} </span>
                            <span>{objFromMoneyArr.number}</span>
                        </li>
                    );
                })}
            </ul>
            <div style={{marginLeft: '30px'}}>
                <button onClick={() => props.onClickFilterHandler('All')}>all</button>
                <button onClick={() => props.onClickFilterHandler('Ruble')}>rubles</button>
                <button onClick={() => props.onClickFilterHandler('Dollar')}>dollars</button>
            </div>
        </>

    );
}*/
